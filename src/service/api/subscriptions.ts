import { IRequestStoreSubscription } from '~/server-side/subscription'

// import { apiService } from './api.service'

export async function createSubscriptions(_subscriptions: IRequestStoreSubscription[]) {
  return null
}
